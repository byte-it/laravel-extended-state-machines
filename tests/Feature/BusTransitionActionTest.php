<?php
test('a chain should be dispatched');
test('the should end with a transition finishing job');
test('a failed chain should cancel the transition');


test('a batch should be dispatched');
test('a batch should have an then callback to finish the transition');
test('a failed batch should cancel the transition');
